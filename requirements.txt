# DUET runtime dependencies
#
# Tested configurations:
# - Python 3.13 / macOS 15.6 (Apple Silicon, MPS)
# - Python 3.12 / Ubuntu 24.04, NVIDIA A100, CUDA 13.0
#
# Backbone: facebook/dinov2-giant. It is public and is downloaded
# automatically by Transformers on first use.
# For offline inference, pass: --model /path/to/dinov2-giant
#
# Install the PyTorch build appropriate for the local platform/CUDA version.
torch>=2.4,<3

# Required by the Hugging Face image-processing stack.
torchvision>=0.18

# Version 4.57.6 was tested. Keep below 5 because DUET relies on the
# Transformers 4.x DINOv2 implementation and hidden-state behaviour.
transformers>=4.57,<5

# **这一条最容易踩坑。** transformers 4.x 硬约束 huggingface-hub>=0.34.0,<1.0。
# 直接 `pip install -U huggingface_hub[cli]` 会装到 1.x,然后 import transformers
# 当场 ImportError。装 CLI 请用:pip install "huggingface_hub[cli]<1.0"
huggingface-hub>=0.34,<1.0

numpy>=1.24,<3
pillow>=10.0
tqdm>=4.65

# ---------------------------------------------------------------------------
# 刻意不依赖的东西,别装:
#
#   accelerate  —— 只有 device_map="auto" 需要它,本项目用 .to(device),不需要
#   scikit-learn —— AUC 用秩法手写(train_WeightandExpert.py / probe_layers.py),
#                   为一个函数引入 sklearn 不值得
#   pandas / pyarrow —— meta 与 manifest 都用 CSV。5 万行 CSV 也就几 MB,还能直接 grep
#   opencv       —— 退化算子全部用 PIL 实现
#
# 画 Figure 2(层级热力图)时再按需装,probe_layers.py 本身只输出 CSV 和文本:
#   matplotlib>=3.7
# ---------------------------------------------------------------------------
